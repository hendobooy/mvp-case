version: '3.8'

services:
  nocodb:
    image: nocodb/nocodb:latest
    container_name: mrv-nocodb
    ports:
      - "8081:8080"
    environment:
      - NC_ALLOW_LOCAL_HOOKS=true
    volumes:
      - nocodb_data:/usr/app/data/
    restart: always

volumes:
  nocodb_data: